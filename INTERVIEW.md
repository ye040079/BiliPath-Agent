# 🎤 面试问答对（逐模块准备）

> 用法：面试前通读一遍，重点记每个「追问」的答法。核心心法——**先讲架构再讲细节，主动暴露难点，用「为什么」证明你真的懂，而不是只会调库**。

---

## 0. 一句话定位

**问**：介绍一下这个项目？

**答**：一个把「视频推荐」做成「可信学习规划」的 Multi-Agent 系统。用户说想学什么，系统不只去 B 站搜视频，还会**审查视频实际讲了什么**、**分析评论区真实口碑**，过滤掉标题党，最后才生成三阶段学习计划。编排层用 LangGraph，工具层自研。

---

## 1. 为什么用 LangGraph

**问**：为什么用 LangGraph，而不是手写 if-else / LangChain / CrewAI？

**答**：三点原因。
1. **我的业务本质是一张 DAG**（意图 → 搜索 → 总结∥评论 → 审查 → 计划），用图来表达最自然，状态流转一目了然。
2. **并行和流式是框架原生能力**。手写时要自己用 `ThreadPoolExecutor` + 自己管 SSE 队列，容易出 bug；LangGraph 的 fan-out/fan-in 和 `astream` 直接给我。
3. **CrewAI/AutoGen 适合"角色对话式多 Agent"**，我的场景是固定管道，用它们杀鸡用牛刀。

**追问**：LangGraph 的并行是怎么真正并行起来的？

**答**：关键在 **async 节点 + `astream`**。同步的 `stream()` 里，同一 superstep 的分支是串行跑的；只有节点写成 `async def`、用 `graph.astream()`，两个没有依赖的节点才会被 `asyncio.gather` 并发调度。我建图时用 `add_edge("search","summarize")` 和 `add_edge("search","analyze_comments")` 制造 fan-out，再用 `add_edge(["summarize","analyze_comments"],"review")` 做 fan-in——review 会等两个分支都完成才执行。

---

## 2. 结构化输出

**问**：LLM 输出不可靠，你怎么保证格式？

**答**：这是我最花心思的点。LLM 返回 JSON 经常带 ```json``` 代码块、前后废话、甚至字段类型不对。我做了三层：
1. **提示词约束**：每个 Agent 的 prompt 都强制"只输出 JSON，不要解释"。
2. **鲁棒提取**：`_extract_json` 先剥代码块，再找第一个 `{`/`[` 做括号配对提取，而不是死板 `json.loads`。
3. **Pydantic 校验 + 自纠错重试**：`complete_structured` 用 `TypeAdapter` 校验 schema（对象或数组都行），校验失败就把错误信息**回喂给模型**，让它看错误自己改，而不是盲目重试。

**追问**：为什么不用 `response_format: json_object`？

**答**：`json_object` 模式只能保证返回 JSON **对象**，但我有节点要返回 JSON **数组**（视频总结是数组），而且不同后端兼容性不一。用「指令 + 提取 + Pydantic 校验 + 回喂重试」这套更通用、可移植，也不依赖具体模型的 function calling 能力。

---

## 3. 流式进度（SSE）

**问**：前端实时进度条怎么实现的？

**答**：后端 `graph.astream(stream_mode="updates")` 每完成一个节点就 yield 一次 `{节点名: 状态更新}`，我把它映射成 `{stage, message, percent}`，包装成 `data: ...\n\n` 的 SSE 事件推给前端；前端用 `fetch` 的流式 reader 逐行解析 `data:` 前缀更新进度条。**流式是 LangGraph 原生给的，不是我手撸的**。

**追问**：之前你提到修过一个 SSE 的 bug，是什么？

**答**：旧代码用 `run_in_executor` 在线程里跑 agent，进度回调里又调 `asyncio.get_event_loop()`——Python 3.12 里子线程拿不到主循环，进度事件进了个永远不会被消费的 loop，所以进度条一直是坏的。改成 `astream` 后，直接在事件循环里 `async for`，这个问题自然消失了。

---

## 4. 视频内容审查（核心卖点）

**问**：怎么判断一个视频"真的讲了这个"而不是标题党？

**答**：分两层。
1. **内容层**：优先抓字幕（`get_video_subtitles`），让 LLM 总结"实际讲什么"，输出 `match_score`（1-10）；没字幕就用标题+简介+标签推断，并标注 `confidence: low`。
2. **审查层**：review 节点按加权（内容相关性 40% / 内容质量 25% / 口碑 20% / 适合人群 15%）综合排序，**过滤 match_score < 5 的**。

**追问**：举个实际例子？

**答**：用户搜"养猪流"，字面上可能指游戏里的某种打法，跟"学习养猪"完全两回事。内容审查会看视频实际讲的是游戏还是养殖，把游戏视频过滤掉。这就是"过滤标题相关但内容不匹配"的典型场景。

---

## 5. 知识库缓存

**问**：缓存怎么做的？会不会命中错主题？

**答**：SQLite 存计划，`find_similar_plan` 两级匹配：先按归一化主题（去标点/空格/大小写）做 MD5 精确匹配；没命中再对最近 20 条按"字符重叠度 ≥ 0.6"做模糊匹配。搜索结果的缓存是 24h TTL，带过期判断。计划缓存命中后直接返回，省掉整条 pipeline。

**追问**：为什么用 SQLite 而不是 Redis？

**答**：这个项目是单机/小规模部署，SQLite 零运维、零依赖，而且我本来就要用它存用户/收藏/记忆，一个库搞定。真要高并发多副本，再换 Redis 也不迟——这是"够用就好"的权衡，不是不会。

---

## 6. 多用户与认证

**问**：多用户怎么隔离的？

**答**：三层。
1. **认证**：PBKDF2-HMAC-SHA256（60 万次迭代）+ 每用户随机盐存密码，登录发随机 Bearer Token（7 天过期）。
2. **会话隔离**：每个 session 一个独立 `StudyAgent` 实例，`session_manager` 用字典 + 线程锁管理，带超时回收。
3. **数据隔离**：收藏表加 `user_id` 列，所有查询按用户过滤；接口全部要登录。

**追问**：为什么 PBKDF2 而不是 bcrypt/argon2？

**答**：PBKDF2 是标准库 `hashlib.pbkdf2_hmac` 直接支持，零额外依赖，且 OWASP 认可（SHA256 推荐 60 万次迭代量级）。bcrypt/argon2 更"现代"，但要多装库、且我对它们参数调优不如 PBKDF2 有把握。安全的核心是"加盐 + 高迭代 + 不存明文"，算法选型其次。

**追问**：API Key 在多用户下怎么处理，避免串号？

**答**：这是我特意改的点。旧代码把 `config.LLM_API_KEY` 当全局变量改，并发下 A 用户的 key 可能被 B 用。现在**每次请求按当前用户解析 key（用户自定义优先，否则服务端默认），通过构造函数注入 `LLMClient`，再 `bind_llm` 绑定到该会话的 agent**，全程不碰全局变量。

---

## 7. B站 wbi 签名

**问**：B站接口有反爬，你怎么处理的？

**答**：搜索接口 `/wbi/search/type` 需要 wbi 签名，否则 412。我实现了完整链路：从 nav 接口拿 `img_key`+`sub_key` → 用固定重排表混淆成 `mixin_key` → 请求参数加时间戳 `wts` 排序 → 过滤特殊字符 → URL 编码 → `md5(query + mixin_key)` 得到 `w_rid`。密钥缓存 1 小时。

**追问**：无兜底是什么意思？

**答**：这是项目的设计原则——B站 API 或 LLM 失败时，**明确抛异常报错，而不是返回假数据或编造结果**。宁可告诉用户"没搜到/调用失败"，也不拿标题瞎编推荐。可信度是这类工具的核心价值。

---

## 8. 歧义确认

**问**：用户输入有歧义怎么办？

**答**：`understand_intent` 节点让 LLM 同时输出 `confidence`，低于阈值（0.7）就输出 `action: clarify` 和具体问题；图上用条件边 `route_after_intent` 判断——clarify 就直接 `END`，返回问题给前端；用户回答后，下一轮把「原始问题 + 回答」合并重跑，并强制 `action=execute` 不再追问。

---

## 9. 安全

**问**：安全上做了什么？

**答**：密钥走环境变量（`.env` 不进库，`.gitignore` 忽略）；前端用户消息和纯文本消息一律转义（防 XSS）；所有 SQL 用参数化查询（`?` 占位符，防注入）；所有数据接口都要登录；收藏按用户隔离。之前硬编码过一个 API Key，已移除并建议吊销。

---

## 10. 工程化

**问**：这个项目工程上完整吗？

**答**：有单元测试（LLM 客户端 / 认证 / 知识库，`pytest`）、Dockerfile + .dockerignore、`.env.example`、requirements 锁定、README。测试覆盖了最容易出错的 JSON 提取和结构化输出重试逻辑。

---

## 11. 可能的深挖（提前想好）

| 面试官可能问 | 你的回答方向 |
|---|---|
| 为什么不用 LangGraph 的 `interrupt()` 做歧义确认？ | 我用条件边 + 会话内状态实现，更简单可控；`interrupt()` 需要 checkpointer 持久化，对单机 demo 是过度设计，但我知道它可以做真正的人机协同暂停/恢复 |
| 为什么自研 LLM 客户端，不用 `langchain-openai`？ | 减少抽象层依赖、更好控制重试/超时/中断；且便于单测。LangGraph 只负责编排，调用层保持轻量 |
| 限流怎么做的？ | `usage_log` 表记录每次使用，按 `DATE(created_at)=DATE('now')` 统计当天次数，超过 `DAILY_LIMIT_PER_USER` 就 429 |
| 长期记忆怎么用的？ | `learning_history` 记录历史主题，下次检索相关记忆注入；当前主要服务 ReAct 类场景，主流程暂未深度使用（诚实说明） |
| 如果视频没字幕怎么办？ | 降级到标题+简介+标签推断，并标注低置信度，审查层权重会相应降低可信度 |
| 并发下 SQLite 会不会锁？ | 每次操作独立开连接、及时关闭，写冲突概率低；生产多写可开 WAL 或换库 |

---

## 12. LLM / Agent 岗位特供问答（投递该方向必看）

### Q1 "这不就是个固定流程吗？跟"调 prompt 的 demo"有什么区别？"

固定编排是**刻意选择**：学习规划场景要可预期、可回溯——审查/排序/输出每一环都可校验，我不会让模型自由乱调工具碰运气。三点区别：
1. 每个环节是**独立可校验的信号**（字幕实际内容、评论区口碑），不是把问题丢给模型一个 prompt 完事；
2. 输出走 **Pydantic 结构化校验**，模型格式错、字段缺会被抓住并回喂重试；
3. 架构上是 LangGraph——若将来要"自主探索"，加一个工具调用节点 + 循环边（或 `create_react_agent`）就能升级成真正自主的 Agent，流程可进化。

### Q2 "ReAct / function calling 你会吗？"

能讲清 **Thought → Action → Observation** 循环：模型每轮输出"思考 + 选工具 + 参数"，把工具结果作为下一轮观察，直到给出 final_answer；要点是**自我纠错**（工具失败就把错误喂回去，让它换策略）和**轮数上限**（防死循环）。
Function calling 本质是让工具参数走 JSON Schema，约束模型输出格式——我在项目里用 **Pydantic 校验 LLM 输出**是同一套思路（把输出约束成机器可校验的结构），区别是 function calling 由模型侧原生支持、更稳。真要自主模式，LangGraph 的 `create_react_agent` 是现成的。

### Q3 "怎么证明你的推荐 / 计划是好的？"

诚实回答：系统化评测是我的下一步，方向明确：
1. **可观测指标**：记录 LLM JSON 解析失败率、审查过滤率（多少视频被判"标题党"）、缓存命中率、平均耗时——直接回答"结构化输出稳不稳""缓存省了多少调用"；
2. **样例评测集**：准备 10-20 个典型学习需求，人工标注理想推荐，做相关性对比。
项目内已有的"软证据"：内容审查就是防"养猪流"这类**标题相关但内容不匹配**的误推——这类 case 本身就是质量机制的证明。

### Q4 "知识库为什么不用 RAG / 向量库？"

取舍问题。我的缓存场景是**整段计划复用**：用户问过"线性代数"就整份复用，关键词归一化 + 哈希够用、零向量化成本、完全可解释。
RAG/向量检索的价值在**大规模资料库按语义召回片段**（比如给计划补充参考资料），那才需要 embedding + 向量库。能说清"什么场景才需要"，比硬上向量库加分。

### Q5 "成本怎么控制？（LLM 按量计费）"

三层：
1. **缓存**：相似计划直接复用，一次生成省掉整条 pipeline 的 5-8 次 LLM 调用；
2. **省着调**：不是所有搜索到的视频都爬字幕+评论——先按标题粗筛再精审；评论只取高赞前 20 条喂模型；字幕截断到 3000 字；
3. **配额**：每用户每日限流（`usage_log` 计数），防单用户打爆。
补一句：**LLM Agent 里最贵的是 token 不是服务器**——成本意识是加分项。

### Q6 "你这项目太单机了，高并发怎么办？"

不硬吹，按"已考虑 + 演进路径 + 瓶颈认知"三层答：
- **已考虑并发**：asyncio、会话级线程锁、每用户每日限流、per-user API Key 依赖注入（不碰全局变量）；
- **演进路径**：uvicorn 多 worker → 内存会话迁 Redis（`SessionManager` 接口已封装，换存储即可）；SQLite 计划缓存 → Redis（隔离在 `KnowledgeBase` 内，可平滑替换）；多实例共享状态、SSE 连接规模再上独立推送通道；
- **关键认知**：这类应用的吞吐瓶颈是 **LLM 调用（秒级延迟 + 限流 + 计费）而不是 HTTP**，所以"高并发"的工程核心是**并发控制外部 LLM + 结果复用 + 队列削峰**，而不是堆服务器。

---

## 13. 诚实清单（被问"你自己写的吗"）

如果被问到代码是否原创：**大大方方说"用了 AI 辅助 + LangGraph 框架，但架构设计、工具层、调试是我主导"**。面试官不在乎谁敲的键盘，在乎你能不能讲清楚**为什么这么设计、遇到什么问题、怎么解决的**——上表每一条都是你可以真讲的东西。
